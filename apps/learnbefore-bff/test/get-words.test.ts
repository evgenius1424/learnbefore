import { test } from "vitest"

test("getWords test", async () => {
  // const openai = new OpenAI({
  //   apiKey: process.env.OPENAI_API_KEY,
  // })
  //
  // const messageId = crypto.randomUUID()
  // const text =
  //   "The contentious debate, marked by acerbic rhetoric and impassioned pleas, underscored the deepening schism within the political landscape. " +
  //   "Amidst the cacophony of accusations, the opposition caucus lambasted the government for its purported complicity " +
  //   "in exacerbating the prevailing economic downturn, attributing it to a series of misguided policies that favored corporate interests over the welfare of the populace."
  //
  // let wordCount = 0
  // for await (const word of getWords(messageId, openai, text)) {
  //   wordCount++
  //   console.log(word)
  // }
  //
  // expect(wordCount).toBeGreaterThan(1)
})
